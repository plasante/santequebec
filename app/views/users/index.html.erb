<h1>Tous les usagers</h1>

<%= will_paginate %>

<ul class="users">
  <% @users.each do |user| %>
    <li>
      <%= link_to user.first_name + ' ' + user.last_name, user %>
    </li>
  <% end %>
</ul>

<%= will_paginate %>
